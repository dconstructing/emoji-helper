<Page loaded="loaded">
	<StackLayout orientation="vertical">
		<ListView items="{{ displayableEmojiList }}">
			<ListView.itemTemplate>
				<Button tap="buttonTapped" text="{{ value }}" />
			</ListView.itemTemplate>
		</ListView>
	</StackLayout>
</Page>
