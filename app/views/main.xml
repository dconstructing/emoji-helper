<Page loaded="loaded">
	<StackLayout orientation="vertical">
		<Button tap="buttonTapped" text="😄" />
		<Button tap="buttonTapped" text="👽" />
		<Button tap="buttonTapped" text="🐬" />
	</StackLayout>
</Page>
